export interface DriverResponse {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    role: string;
    active: boolean;
    licenseNumber: string;
    phoneNumber: string;
    available: boolean;
}
